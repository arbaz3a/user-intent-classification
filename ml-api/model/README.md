# Model Directory

Place the following pre-trained model files in this directory:
- `model.pkl` — Trained classifier (LinearSVC / LogisticRegression)
- `tfidf.pkl` — Fitted TF-IDF vectorizer
- `label_encoder.pkl` — Fitted LabelEncoder

These files are loaded by `predict.py` at startup.
